services:
  - type: web
    name: webtaixiu
    env: node
    plan: free
    buildCommand: rm -rf node_modules yarn.lock && yarn install && yarn build
    startCommand: yarn start
    autoDeploy: true
